<section class="comu-hero">
  <img class="comu-logo" src="/imgs/comu-logo.webp" alt="COMU logo" loading="lazy" />

  <h1>A COMMUNITY OF CURL-FOCUSED STYLISTS</h1>
</section>

<style>
  .comu-hero {
    height: 40vh;
    background-image: linear-gradient(rgba(0, 0, 0, 0.15), rgba(0, 0, 0, 0.15)), url("/imgs/comu-hero.webp");

    background-size: cover;
    background-position: center;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    text-align: center;
  }

  .comu-logo {
    width: min(60vw, 520px);
  }

  .comu-hero h1 {
    margin-top: -1rem;
    color: var(--accent);
    font-size: clamp(1rem, 2vw, 2rem);
    font-style: italic;
    font-family: sans-serif;
    font-weight: 400;
    /* text-transform: uppercase; */
    letter-spacing: 0.03em;
  }

  @media (min-width: 768px) {
    .comu-hero {
      height: 60vh;
    }

    .artist-grid {
      grid-template-columns: repeat(3, 1fr);
      align-items: start;
    }

    .image-grid {
      grid-template-columns: repeat(2, 1fr);
      align-items: stretch;
      gap: 3rem;
    }
  }

  @media (min-width: 900px) {
    .comu-hero {
      height: 50vh;
    }
  }
</style>
