<section class="comu-hero">
  <img class="comu-logo" src="/imgs/comu-logo.png" alt="COMU logo" />

  <p>A COMMUNITY OF CURL-FOCUSED STYLISTS</p>
</section>

<style>
  .comu-hero {
    height: 40vh;
    background-image: linear-gradient(rgba(0, 0, 0, 0.15), rgba(0, 0, 0, 0.15)), url("/imgs/comu-hero.JPG");

    background-size: cover;
    background-position: center;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    text-align: center;
  }

  .comu-logo {
    width: min(60vw, 520px);
  }

  .comu-hero p {
    margin-top: -1rem;
    color: var(--accent);
    font-size: clamp(1rem, 2vw, 2rem);
    font-style: italic;
    font-family: sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }
</style>
